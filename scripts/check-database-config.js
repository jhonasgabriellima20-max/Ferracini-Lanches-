const configured=Boolean(process.env.DATABASE_URL||process.env.POSTGRES_URL); console.log('FERRACINI_DATABASE_CONFIGURED',configured); if(!configured){console.error('Conecte o Neon ao ambiente Preview do projeto Ferracini.');process.exit(42);}
